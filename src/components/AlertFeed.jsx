import { AlertTriangle } from 'lucide-react'
export default function AlertFeed({alerts=[],onOpen}){if(!alerts.length)return <div className="text-sm text-slate-500">No active alerts.</div>;return <div className="space-y-2">{alerts.slice(0,6).map((a,i)=><button key={a.id||i} onClick={()=>onOpen?.(a.shipment_id||a.shipmentId)} className="flex w-full items-start gap-3 rounded-xl border border-slate-700/50 bg-slate-950/20 p-3 text-left"><AlertTriangle size={15} className={a.severity==='critical'?'text-red-300':'text-amber-300'}/><div><div className="text-sm font-semibold">{a.title||a.alert_type||'Cold-chain alert'}</div><div className="mt-1 text-xs text-slate-500">{a.message||a.recommended_action||''}</div></div></button>)}</div>}
